<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="32dp">

    <EditText
        android:id="@+id/editPetName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Pet Name" />

    <EditText
        android:id="@+id/editPetAge"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Pet Age"
        android:inputType="number" />

    <EditText
        android:id="@+id/editPetRendezvous"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Date du rendez-vous (cliquer pour choisir)"
        android:focusable="false"
        android:clickable="true"

        android:padding="14dp"
        android:textSize="16sp"
        android:layout_marginBottom="16dp" />

    <!-- You can add an ImageView or ImagePicker for selecting pet photo -->
    <ImageView
        android:id="@+id/imageViewPetPhoto"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:src="@drawable/ic_pet_photo" />

    <Button
        android:id="@+id/btnSavePet"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Save Pet" />

</LinearLayout>
